## Packages
framer-motion | Page transitions and scroll-triggered animations
date-fns | Human-readable date formatting

## Notes
- App uses Replit Auth for authentication. All login/logout actions redirect to /api/login and /api/logout.
- User profile (Aadhaar number) must be completed before users can submit new issues.
- Issues API expects image URLs as simple text strings.
